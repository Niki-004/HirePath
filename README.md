# Finance Tracker (Backend)

Tech: Java, Spring Boot, Maven, Docker

Backend service for a personal finance tracker (work in progress).  
Implements the main Spring Boot structure and configuration for recording expenses/budgets.  
Includes Dockerfile and deployment setup for Render (deployment in progress).
