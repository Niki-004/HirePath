# ===== Backend (Spring Boot) =====
SPRING_PROFILES_ACTIVE=dev

# If you switch to PostgreSQL later:
DB_HOST=localhost
DB_PORT=5432
DB_NAME=jobtracker_db
DB_USER=jobtracker_user
DB_PASSWORD=your_password_here

# Optional (future)
JWT_SECRET=change_me_later
